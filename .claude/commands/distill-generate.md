Generate an optimized .llmignore and CLAUDE.md for the current project.

```bash
python3 -m scripts.generate_config --path .
```

After running, show the contents of the generated files and explain what each ignore rule eliminates and why.
