# skim-llm — Python dependencies
# Core proxy + scanner work with stdlib only (zero hard deps).
# Install optional groups for full functionality.

# Accurate token counting (strongly recommended)
tiktoken>=0.7.0

# LLM provider SDKs (install what you use)
anthropic>=0.40.0
openai>=1.50.0
google-generativeai>=0.8.0

# Web dashboard (skim server)
flask>=3.0.0
passlib>=1.7.4

# Enterprise auth — optional
# authlib>=1.3.0   # OIDC/OAuth2 SSO
# ldap3>=2.9.0     # LDAP / Active Directory
# gunicorn>=21.0   # Production WSGI server (recommended over Flask dev server)

# MCP server integration
# mcp>=1.0.0

# Dev / testing
pytest>=8.0.0
