# TokenWise — Python dependencies
# Core tools work with stdlib only (no hard deps)
# Install optional packages for full functionality

# Accurate token counting (strongly recommended)
tiktoken>=0.7.0

# Claude support
anthropic>=0.40.0

# OpenAI / OpenAI-compatible API support
openai>=1.50.0

# Google Gemini support
google-generativeai>=0.8.0

# Dev / testing
pytest>=8.0.0
